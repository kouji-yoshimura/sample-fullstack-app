model Post {
  postId     String     @id
  threadId   String
  thread     Thread
  postNumber Int
  body       String
  createdBy  String
  updatedBy  String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @default(now())
}
